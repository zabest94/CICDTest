name: PyTest

on:
  push:
    branches:
      - 'main'


json:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test with pytest
        run: |
          python3 test.py